<section id="login">
  <div class="login-wrapper">
    <h2>تشجل</h2>
    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>
      <div class="form-inputs">
        <div class="group">
          <%= f.input :first_name,
                      required: true,
                      autofocus: true , wrapper: false, label: false%>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :first_name, label: 'سمیتك', class: 'material' %>
        </div>
        <div class="group">
          <%= f.input :last_name,
                      required: true,
                      autofocus: true , wrapper: false, label: false%>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :last_name, label: 'كنیتك', class: 'material' %>
        </div>
        <div class="group">
          <%= f.input :country_code, as: :select,  input_html: {id: 'authy-countries', data: {show_as: 'number', value: '+212'}},
                      required: true,
                      autofocus: true , wrapper: false, label: false%>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :country_code, for: 'country-code-0', label: 'الكود دیال لبلاد', class: 'material' %>
        </div>
        <div class="group">
          <%= f.input :phone,
                      required: true,
                      autofocus: true , wrapper: false, label: false%>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :phone, label: 'نمرة دیال تلفون', class: 'material' %>
        </div>
        <div class="group">
          <%= f.input :password,
                      required: true,
                      input_html: { autocomplete: "new-password" } ,
                      autofocus: true, wrapper: false, label: false %>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :password, label: 'الكود', class: 'material' %>
        </div>
        <div class="group">
          <%= f.input :password_confirmation,
                      required: true,
                      input_html: { autocomplete: "new-password" } ,
                      autofocus: true, wrapper: false, label: false %>
          <span class="highlight"></span>
          <span class="bar"></span>
          <%= f.label :password_confirmation, label: 'عاود الكود', class: 'material' %>
        </div>
      </div>
      <div class="form-actions flex-center">
        <%= f.button :submit, "تشجل", class: 'btn btn-lg btn-success btn-rCab' %>
      </div>
    <% end %>
    <%= render "devise/shared/links" %>
  </div>
</section>
